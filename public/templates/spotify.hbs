<h2 class="widget-title"> Spotify </h2>
<div class="widget-content">
    {{#if data}}
        
        
        <p class="widget-heading">
            {{#if data.playing}}Now Playing{{else}}Paused{{/if}}
        </p>
        
        <h3> {{#if data.playing}}&#9654;{{/if}} {{data.track}} </h3>
        <p>&dash; {{data.artist}}</p>
        
    {{else}}
        <p> <a href="auth/spotify" class="button"> Please login </a> </p>
    {{/if}}
</div
